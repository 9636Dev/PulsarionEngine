int min(int, int a);
struct vec3;

int a = static_cast<int>(1.5f);
[[annotated]] double b = 0.0;
float c = 1.2f;
vec3 d(1,2,3);

//a + b > c == true && d || e
//a == d || e

//a && b || c == d
//a++ + ++b * c[2] > d && e <= f.j(-1, 2) || (--min(a, g) == h::i::j.k.l--);


int min(int a, int b)
{
}

struct vec3 {
    float a;
    float b;
    float c;
}
