#include "vec3.psh"
#include "baseVec.psh"
int min(int, int a);

int a = static_cast<int>(1.5f);
[[annotated]] double b = 0.0;
float c = 1.2f;
vec3 d(1,2,3);

//a == d || e;
//a + b > c == true && d || e
//a && b || c == d
a++ + ++b * c[2] > d && e <= f.j(-1, 2) || (--min(a, g) == h::i::j.k.l--);


int min(int a, int b)
{
    if (a < b)
    {
        return a;
    }

    if (b < a)
        return b;

    return a < b ? a : b;
}

struct vec3 {
    float a;
    float b;
    float c;
}

